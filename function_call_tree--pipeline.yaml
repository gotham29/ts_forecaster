run_pipeline
  validate_config
    make_dir
  split_data
  save_data
  if train_models:
    train_save_models
      gridsearch_model
    get_modnames_preds
      get_model_lag
    get_modnames_evals
      get_eval
    get_model_best
    save_results
    save_results
  else:
    load_models
    get_modnames_preds
      get_model_lag
  save_data

